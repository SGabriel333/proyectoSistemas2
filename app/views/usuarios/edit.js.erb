$('#ver').html('<%= j  (render "edit")%>');
$('#myModal').modal({
              modal: true,
              buttons: {
                Ok: function() {
                  $( this ).modal( "close" );
                }
              }
            });
$('#ver').slideDown(350);